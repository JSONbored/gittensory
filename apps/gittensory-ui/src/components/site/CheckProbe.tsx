// Demo component to verify Reviewbot's check run — safe to delete.
export function CheckProbe({ label }: { label: string }) {
  return <span className="text-xs text-muted-foreground">{label}</span>;
}
